command = /usr/bin/fish --login --interactive

# Show menu
window-decoration = client

# Install script injects this to ghostty config
# gtk-custom-css = /home/konso/.config/ghostty/toast-more-margin.css

# Not supported on Linux (25-05)
# window-save-state = always

# Ctrl keys remap
keybind = ctrl+shift+c=text:\x03
keybind = ctrl+c=text:\x03
keybind = performable:ctrl+c=copy_to_clipboard
keybind = ctrl+v=paste_from_clipboard
keybind = ctrl+t=new_tab
keybind = ctrl+n=new_window
keybind = ctrl+f4=close_tab
keybind = ctrl+a=select_all
# keybind = ctrl+q=quit

keybind = ctrl+shift+r=reload_config

# Splits
keybind = ctrl+shift+right=unbind
keybind = ctrl+shift+down=unbind
keybind = ctrl+shift+up=unbind
keybind = ctrl+shift+left=unbind
# These conflict with moving cursor word at a time
#keybind = ctrl+up=goto_split:up
#keybind = ctrl+down=goto_split:down
#keybind = ctrl+right=goto_split:right
#keybind = ctrl+left=goto_split:left
# Surface: split or tab
keybind = ctrl+f4=close_surface

# Fix "Error opening terminal: xterm-ghostty"
shell-integration-features = sudo

confirm-close-surface = false
